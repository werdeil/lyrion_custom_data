# Minimal dependencies to run scripts/embed_lyrics.py outside Docker.
# No Flask/gunicorn/Lyrion needed — the CLI works directly on the files.
requests>=2.31
mutagen>=1.47
beautifulsoup4>=4.12   # optional: only needed for the Genius provider
python-dotenv>=1.0     # optional: auto-loads .env so the CLI shares the app config
